[package]
name = "underonunicom/is-even-luau"
description = "A (totally) simple package to check whether or not the number is even or not."
license = "MIT"
version = "1.0"
registry = "https://github.com/UpliftGames/wally-index"
realm = "shared"
exclude = ["aftman.toml","CHANGELOG.md"]

[dependencies]